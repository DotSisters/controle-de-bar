@{
  Layout = "_Layout";
  var currentAction = ViewContext.RouteData.Values["action"]?.ToString() ?? "";
}

@if (currentAction == "Listar")
{
  @RenderBody()
}
else
{
  <div class="crud-panel">
    @RenderBody()
  </div>
}
